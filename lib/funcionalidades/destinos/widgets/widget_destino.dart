import 'package:flutter/material.dart';

class WidgetDestino extends StatelessWidget {
  const WidgetDestino({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
