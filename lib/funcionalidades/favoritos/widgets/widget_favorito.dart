import 'package:flutter/material.dart';

class WidgetFavorito extends StatelessWidget {
  const WidgetFavorito({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
