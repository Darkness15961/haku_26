import 'package:flutter/material.dart';

class WidgetRuta extends StatelessWidget {
  const WidgetRuta({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
