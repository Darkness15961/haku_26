# Edge Functions de HAKU

Esta carpeta contiene el código fuente versionado de las Supabase Edge
Functions.

Estructura esperada:

```text
supabase/functions/
  nombre-funcion/
    index.ts
```

`bunny-ticket` está versionada localmente y adaptada al esquema actual de
publicaciones. Antes de probar video desde Flutter debe desplegarse esta versión
en el servidor y conservar configurados sus secretos de Bunny Stream.
